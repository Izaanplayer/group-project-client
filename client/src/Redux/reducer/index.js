const initialState = {
    artistDetail: []
};

function rootReducer(state = initialState, action) {
    switch (action.type) {
        case '':

            break;

        default:
            return state;
    }
}